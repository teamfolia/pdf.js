import {projects} from "paper";
import * as uuid from 'uuid'

class FoliaProxy {
  annotations
  apiEventBus

  constructor(apiEventBus, pdfEventBus) {
    this.annotations = []
    this.eventBus = eventBus
  }

}

export default FoliaProxy
